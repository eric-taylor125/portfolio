<%= form_for(@industrium) do |f| %>
  <% if @industrium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@industrium.errors.count, "error") %> prohibited this industrium from being saved:</h2>

      <ul>
      <% @industrium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
