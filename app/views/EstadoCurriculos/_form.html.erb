<%= form_for(@estado_curriculo) do |f| %>
  <% if @estado_curriculo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estado_curriculo.errors.count, "error") %> prohibited this estado_do_curriculo from being saved:</h2>

      <ul>
      <% @estado_curriculo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
