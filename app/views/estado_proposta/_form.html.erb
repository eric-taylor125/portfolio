<%= form_for(@estado_propostum) do |f| %>
  <% if @estado_propostum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estado_propostum.errors.count, "error") %> prohibited this estado_propostum from being saved:</h2>

      <ul>
      <% @estado_propostum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
