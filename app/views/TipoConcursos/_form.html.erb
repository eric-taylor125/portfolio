<%= form_for(@tipo_concurso) do |f| %>
  <% if @tipo_concurso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_concurso.errors.count, "error") %> prohibited this tipo_de_concurso from being saved:</h2>

      <ul>
      <% @tipo_concurso.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
